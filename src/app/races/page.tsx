import type { FC } from "react";

import { Stack, Typography } from "@mui/material";

const Home: FC = () => {
  return (
    <Stack bgcolor="pink">
      <Typography>Races</Typography>
    </Stack>
  );
};

export default Home;
